# Environment
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# ML Models (binary, regeneratable)
*.pkl
models/saved/
predictions/saved_models/

# Market Data
data/historical/
data/jugaad_cache/
predictions/data/

# Logs and Results
logs/
backtest_results/

# OS
.DS_Store

# Reference-only source (original zips + the Market_Scanner tree being
# ported from) — not part of the shipped project, kept locally for lookup.
_vendor-src/

# Node
node_modules/
dashboard/.next/
data/eod_cache/
data/intraday_cache/

# Broker SDK logs, written into the repo root at runtime by tradingapi_a
# (mStock). Tracebacks from failed logins land here, so they are kept out of
# version control rather than risk a session detail being committed by accident.
mconnect.log
mticker.log

# Local backups of the paper-trade journal
*.jsonl.bak
